[project]
name = "clarus-evaluation"
version = "0.1.0"
description = "LLM evaluation framework for Clarus Bias Checker"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.0",
    "openai>=1.0",
    "rich>=13.0",
    "google-genai>=1.0",
    "pysbd>=0.3",
    # "transformers>=4.0",
    # "torch>=2.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]
